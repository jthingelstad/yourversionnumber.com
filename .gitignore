.DS_Store
node_modules/
.vscode/
.idea/
*.log
.playwright-mcp/
design_handoff_theme_refresh/

# Local scratch probes used while building themes — never shipped.
# Any underscore-prefixed HTML at the root is a throwaway render harness.
# The earlier list was per-name and kept letting new ones through.
_*.html
DNS-CUTOVER.md
